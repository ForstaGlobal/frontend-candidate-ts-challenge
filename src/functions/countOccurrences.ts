/**
 * Counts the number of times a subString appears in a string (case-insensitive).
 * @param str The string to check
 * @param subString The subString to count
 * @returns The number of times the subString appears in the string
 *
 * @example
 * countOccurrences("", "") // 0
 * countOccurrences("", "a") // 0
 * countOccurrences("hello world", "") // 0
 * countOccurrences("hello world", "x") // 0
 * countOccurrences("hello Obi", "o") // 2
 * countOccurrences("hello world", " ") // 1
 * countOccurrences("hello, world", ",") // 1
 * countOccurrences("coat cocoa Coke acapulco coop", "co") // 6
 */
export function countOccurrences(str: string, subString: string): number {
  // TODO: Implement this function

  return -1;
}
