/**
 * Finds the positions of all duplicate numbers in an array.
 * @param arr The array to check for duplicates
 * @returns A map of all numbers that appear more than once in the array
 *
 * @example
 * findDuplicatePositions([]) // {}
 * findDuplicatePositions([1]) // {}
 * findDuplicatePositions([1, 2, 3]) // {}
 * findDuplicatePositions([1, 1]) // { 1: [0, 1] }
 * findDuplicatePositions([2, -1, -1]) // { -1: [1, 2] }
 * findDuplicatePositions([1, 2, 0, -2, 3, 0, 1, 2, 5]) // { 0: [2, 5], 1: [0, 6], 2: [1, 7] }
 */
export function findDuplicatePositions(arr: number[]): Record<number, number[]> {
  // TODO: Implement this function

  return {};
}
